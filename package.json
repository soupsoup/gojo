{
  "name": "newsdj-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "A personalized rapid audio news alert prototype.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
