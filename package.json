{
  "name": "newsdj-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "A personalized daily email briefing and produced audio newscast.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check audio-production.js && node --check network-news-theme.js && node --check email-delivery.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@runwayml/sdk": "^4.10.0",
    "ffmpeg-static": "^5.3.0"
  }
}
